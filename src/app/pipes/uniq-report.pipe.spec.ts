import { UniqReportPipe } from './uniq-report.pipe';

describe('UniqReportPipe', () => {
  it('create an instance', () => {
    const pipe = new UniqReportPipe();
    expect(pipe).toBeTruthy();
  });
});
